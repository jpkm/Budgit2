<div id="header">
  <% if logged_in? %>
    <%= link_to(image_tag("Header.png", :border => 0), root_url) %><%= link_to(image_tag("logout.png", :border => 0), logout_path) %>
	<%= current_user.name %>
  <% end %>
</div> 