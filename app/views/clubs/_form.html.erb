<%= form_for(@club) do |f| %>
  <% if @club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@club.errors.count, "error") %> prohibited this club from being saved:</h2>

      <ul>
      <% @club.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <table id="form">
  <div class="field">
    <tr>
		<td><%= f.label :name %></td>
		<td><%= f.text_field :name %></td> 
	</tr>
	<br>
	<tr>
		<td><%= f.label :club_key %></td>
		<td><%= f.text_field :club_key %></td>
	</tr>
  </div>
  <td></td>
  
  <td align="right"><%= f.submit %><%= link_to "or Cancel", clubs_path %></td> 
</table>
<% end %>

