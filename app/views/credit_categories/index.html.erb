<h1>Listing credit_categories</h1>

<table>
  <tr>
    <th>Active</th>
    <th>Category</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @credit_categories.each do |credit_category| %>
  <tr>
    <td><%= credit_category.active %></td>
    <td><%= credit_category.category %></td>
    <td><%= link_to 'Show', credit_category %></td>
    <td><%= link_to 'Edit', edit_credit_category_path(credit_category) %></td>
    <td><%= link_to 'Destroy', credit_category, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Credit category', new_credit_category_path %>
