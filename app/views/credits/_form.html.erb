<%= form_for(@credit) do |f| %>
  <% if @credit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@credit.errors.count, "error") %> prohibited this credit from being saved:</h2>

      <ul>
      <% @credit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.hidden_field :account_id, :value => @credit.account_id %>
  
  <div class="field">
    <%= f.label :category_category %>:
		<% if @answer %>
			<%= f.collection_select :credit_category_id, @except, :id, :category, :prompt => "Select ..." %>
		<% else %>
			<%= f.collection_select :credit_category_id, CreditCategory.all, :id, :category, :prompt => "Select ..." %>
		<% end %>
   </div>
 
 
  </div>
  <div class="field">
    <%= f.label :amount %><%= f.text_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
