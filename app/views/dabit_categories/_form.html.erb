<%= form_for(@dabit_category) do |f| %>
  <% if @dabit_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dabit_category.errors.count, "error") %> prohibited this dabit_category from being saved:</h2>

      <ul>
      <% @dabit_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category %><br />
    <%= f.text_field :category %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
